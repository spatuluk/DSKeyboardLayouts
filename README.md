# DSKeyboardLayouts
XML Keyboard Layouts for use with DS Keyboard Android app.

Currently the layouts are just for use with Sinclair ZX Spectrum emulators, as they require some weird key combos.  

The app doesn't allow changing the colour of shiftLabel, but it'd have the labels the wrong way around, otherwise.  Also, it has what seems like an arbritary 16 column limit, which means 1.5x sized keys are tricky to implement.
